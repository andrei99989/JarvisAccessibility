name: Android APK Build

on:
  push:
    branches:
      - main
      - jarvis-brain-core-2
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Build release APK
        run: gradle assembleRelease

      - name: Copy APK
        run: |
          mkdir -p release
          cp app/build/outputs/apk/release/app-release.apk release/app-debug.apk

      - name: Verify APK version
        run: |
          aapt dump badging release/app-debug.apk | grep "versionCode\|versionName"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: JarvisAccessibility-release
          path: release/app-debug.apk

      - name: Read version name
        id: version
        run: |
          VERSION_NAME=$(grep 'versionName' app/build.gradle | head -1 | sed 's/.*versionName = "\(.*\)".*/\1/')
          VERSION_CODE=$(grep 'versionCode' app/build.gradle | head -1 | sed 's/.*versionCode = \(.*\)/\1/')
          echo "version_name=$VERSION_NAME" >> $GITHUB_OUTPUT
          echo "version_code=$VERSION_CODE" >> $GITHUB_OUTPUT

      - name: Create versioned GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version_name }}
          name: Jarvis Accessibility v${{ steps.version.outputs.version_name }}
          body: |
            Jarvis Accessibility APK

            Version code: ${{ steps.version.outputs.version_code }}
            Version name: ${{ steps.version.outputs.version_name }}
          files: release/app-debug.apk
          make_latest: true
          overwrite_files: true

      - name: Publish APK to stable release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: stable
          name: Stable Jarvis Accessibility APK
          body: |
            Stable Jarvis Accessibility APK

            Version code: ${{ steps.version.outputs.version_code }}
            Version name: ${{ steps.version.outputs.version_name }}
          files: release/app-debug.apk
          make_latest: true
          overwrite_files: true
